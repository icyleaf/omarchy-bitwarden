name: Release Omarchy Plugin

on:
  push:
    tags:
      - "omarchy-bitwarden-*"
      - "omarchy-bitwarden-v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag"
        required: true
        type: string

permissions:
  contents: write

jobs:
  package-plugin:
    name: Package & Publish Plugin
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.tag || github.ref }}
          fetch-depth: 0

      - name: Package Plugin Archive
        run: |
          TAG_NAME="${{ inputs.tag || github.ref_name }}"
          ARCHIVE_NAME="omarchy-bitwarden-${TAG_NAME}"
          mkdir -p dist

          tar -czvf "dist/${ARCHIVE_NAME}.tar.gz" \
            OmarchyBitwarden.qml \
            components \
            scripts \
            manifest.json \
            preview.png \
            LICENSE \
            README.md \
            README.zh-CN.md
          cd dist
          sha256sum "${ARCHIVE_NAME}.tar.gz" > "${ARCHIVE_NAME}.tar.gz.sha256"
          cd ..

      - name: Generate Changelog with git-cliff
        uses: orhun/git-cliff-action@4a4a951bc43fafe41cd2348d181853f52356bee7 # v4.4.2
        with:
          config: cliff.toml
          args: --tag ${{ inputs.tag || github.ref_name }} --tag-pattern "^omarchy-bitwarden-v?[0-9].*" --exclude-path "omawarden/**" --strip header --latest
        env:
          OUTPUT: CHANGELOG.md
          GITHUB_REPO: ${{ github.repository }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Plugin Release
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3.0.3
        with:
          tag_name: ${{ inputs.tag || github.ref_name }}
          name: "${{ inputs.tag || github.ref_name }}"
          body_path: CHANGELOG.md
          files: |
            dist/*.tar.gz
            dist/*.sha256
          draft: false
          prerelease: false
          make_latest: false
